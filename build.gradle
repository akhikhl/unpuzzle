ext {
  group = 'org.akhikhl.mavenize'
  version = '0.0.1'
}

apply from: 'root.gradle'

subprojects {
  apply from: rootProject.file('common.gradle')
  apply from: rootProject.file('sonatype.gradle')
}

